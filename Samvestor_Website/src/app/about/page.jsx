export const metadata = {
    title: 'About — Samvestor',
};

/* Placeholder route (the old #/about page). The navbar comes from the
   root layout; drop the About sections in here when they're ready. */
export default function AboutPage() {
    return <div className="about-page" />;
}
